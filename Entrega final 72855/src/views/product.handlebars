<div class="product-container">
  <h1>{{title}}</h1>
  <section class="product-detail">
    {{#if product}}
      <div class="card-detail">
        <img
          src="{{#if product.photo}}{{product.photo}}{{else}}https://static.thenounproject.com/png/1247947-200.png{{/if}}"
          alt="{{product.title}}"
          class="product-img"
        />
        <div class="product-info">
          <h3>{{product.title}}</h3>
          <p class="description">{{product.description}}</p>
          <p class="price">💲 {{product.price}}</p>
          <p class="stock">📦 Stock disponible: {{product.stock}}</p>

          <div class="button-container">
            <button class="add-to-cart">Add to cart</button>
            <a href="/" class="back-home">Go Back</a>
          </div>
        </div>
      </div>
    {{else}}
      <div class="not-found">
        <h1>¡NOT FOUND PRODUCT!</h1>
        <div class="real-time"><a  href="/">Go back-end</a></div>
      </div>
    {{/if}}
  </section>
</div>
